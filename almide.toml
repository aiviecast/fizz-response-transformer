[package]
name = "fizz_response_transformer"
version = "0.1.0"
description = "Fizz response transformer — kotodama transforms.json catalog + weighted selection: lookup/normalize weights, pick a transform from trigger candidates by weight. Rendering is LLM-side. One Almide core, native + wasm."
